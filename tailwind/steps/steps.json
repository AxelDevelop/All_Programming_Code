{
    "scripts": {
        "script1": "npm init -y",
        "script2": "npm install -D postcss postcss -cli",
        "autoprefixerScript": "npm install -D autoprefixer"
    },
    "filesConfig":{
        "configCss": "Create a new file called postcss.config.js inside the working file and to write module.exports = {plugins:[ require('tailwindcss'),require('autoprefixer')]}",
        "package.json": "add the following sentences to the file called package.json in the part called scripts:  ",
        "scripts": " 'build': 'postcss src/styles.css --output css/styles.css', 'watch': 'postcss src/styles.css --output css/styles.css --watch'",
        "src/Styles": "create a folder inside the work file called src and inside a file called style.css"
    },
    
    "installTailwind":{
        "scripts": "npm i -D tailwindcss"
    },
    "configTailwindFile":{
        "scripts": "npx tailwind init"
    },
    "addTailwindToCss": {
        "CssFile":"@tailwind base; @tailwind components; @tailwind utilities ",
        "runTail":{
            "script1":"npm run dev",
            "script2":"npm run"
        }

    }


    
}